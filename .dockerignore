# Keep the build context small and reproducible: the multistage build installs
# deps and bundles from source, so host build output and VCS state are excluded.
node_modules
dist
.git
data
test
docs
*.log
plandrop-*.tgz
.env
